apiVersion: v1
kind: Service
metadata:
  name: vision-mlops-server
  labels:
    app: vision-mlops-server
spec:
  selector:
    app: vision-mlops-server
  ports:
    - name: http
      port: 80
      targetPort: http
